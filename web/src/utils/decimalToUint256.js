// SPDX-License-Identifier: BUSL-1.1
import { BigNumber, ethers } from 'ethers';
import { BigNumber: BN } from 'bignumber.js';

export default function decimalToUint256(d, decimals) {
    d = new BN
}